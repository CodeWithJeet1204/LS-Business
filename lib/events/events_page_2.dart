import 'package:flutter/material.dart';

class EventsPage2 extends StatelessWidget {
  const EventsPage2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Random'),
      ),
    );
  }
}
